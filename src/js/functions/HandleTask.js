import createObjTask from "./CreateObjTask.js"
import TaskList from "../class/TaskList.js"
import TaskUI from "./TaskUI.js"

export default function handleTask(name){

  console.log("Kawai")
      // // No hay persistencia
      // const taskList = new TaskList(name)
      // const taskUI = TaskUI()

      // const addTask = document.querySelector("#btn-submit")
      // addTask.addEventListener("click",
      //   taskList.add(createObjTask())
      // )

      // taskList.get().forEach(task => taskUI.render(task))
}

